services:
  - type: web
    name: snaky-app
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    plan: starter
    region: oregon
    
    # Environment variables
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: DATABASE_URL
        sync: false

    # Health check endpoint
    healthCheckPath: /docs

    # Auto-deploy on push
    autoDeploy: true
